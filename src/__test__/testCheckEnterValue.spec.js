// Import the js file to test
import { checkEnterValue } from '../client/assets/js/checkEnterValue';

describe('Testing the entered value', () => {
    test('Testing the checkEnterValue() function', () => {
           expect(checkEnterValue).toBeDefined();
})});